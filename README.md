# verification_redis_raft
Tools to easily verification RedisRaft with Docker

## how to use 

```bash
git clone # this repo 
git submodule init
git submodule update
cd redisraft 
git submodule init
git submodule update
 
./cluster_setup.bash
```